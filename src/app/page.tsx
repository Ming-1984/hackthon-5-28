export default function Home() {
  return (
    <main style={{ padding: 32, fontFamily: "sans-serif" }}>
      <h1>ChronoForge Agent MVP</h1>
      <p>POST world parameters to <code>/api/generate</code> to run 6 concurrent Qwen agents.</p>
    </main>
  )
}
